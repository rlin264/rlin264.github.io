// src/components/BlogList/LoadingState.tsx
import React from 'react';

const LoadingState: React.FC = () => {
  return (
    <div className="dispatch-loading">loading posts...</div>
  );
};

export default LoadingState;
